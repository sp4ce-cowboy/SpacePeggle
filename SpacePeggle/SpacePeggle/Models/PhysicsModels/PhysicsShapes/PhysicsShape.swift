import Foundation

/*
 /// A custom shape type that all physics objects will have.
 /// This allows for a common intersection interface between all PhysicsObjects.
 protocol PhysicsShape: UniversalShape {
 var width: Double { get set }
 var height: Double { get set }
 func intersects(with shape: any PhysicsShape) -> Bool
 
 mutating func updateCenter(to position: Vector)
 }
 
 // Implement this method in all conforming shapes.
 extension PhysicsShape {
 mutating func updateCenter(to position: Vector) {
 self.center = position
 }
 }
 */
